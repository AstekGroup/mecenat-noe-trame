/**
 * Re-export all types from the shared @make-map/types package.
 * This allows existing component imports (from '@/types/project') to keep working
 * while using the shared type definitions.
 */
export * from '@make-map/types';
